{% set name = "lightkurve" %}
{% set version = "1.0b19" %}
{% set sha256 = "a347257f56d77e323314c03f2f70adbcafaedb2ecf955a78abb4406c0aec8755" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps --ignore-installed --no-cache-dir -vvv"

requirements:
  host:
    - astropy
    - astroquery
    - beautifulsoup4
    - matplotlib
    - numpy >=1.10  # required by autograd via oktopus
    - oktopus
    - pandas
    - bokeh
    - pip
    - pytest-runner
    - python
    - requests
    - scipy
    - tqdm
  run:
    - astropy
    - astroquery
    - beautifulsoup4
    - matplotlib
    - {{ pin_compatible('numpy') }}
    - oktopus
    - bokeh
    - pandas
    - python
    - requests
    - scipy
    - tqdm

test:
  imports:
    - lightkurve

about:
  home: https://lightkurve.keplerscience.org/
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: 'A beautiful package for astronomical flux time series analysis in Python'

  description: |
    The lightkurve Python package offers a beautiful and user-friendly way to
    analyze astronomical flux time series data, in particular the pixels and
    lightcurves obtained by NASA’s Kepler, K2, and TESS missions.
  doc_url: https://lightkurve.keplerscience.org/
  dev_url: https://github.com/KeplerGO/lightkurve

extra:
  recipe-maintainers:
    - gully
    - pkgw
